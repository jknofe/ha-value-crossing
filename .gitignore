# Agent working memory — separate repo, never committed to the project repo
.ai/

# Claude Code agent scaffold (local only, never committed to the public repo)
.claude/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
/build/
/dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Secrets / keys — never commit (this repo is public)
*.key
ha.key*
*.pem
*.p12
secrets.yaml
.env

# OS / editor
.DS_Store

# Agent instruction files (local only, not in the public repo)
/AGENTS.md
/CLAUDE.md
